{
  "entities": {
    "Monster": {
      "title": "Monster",
      "description": "Information about a D&D monster cached from aidedd.org",
      "type": "object",
      "properties": {
        "slug": { "type": "string", "description": "The unique slug for the monster" },
        "name": { "type": "string", "description": "The name of the monster" },
        "htmlContent": { "type": "string", "description": "The HTML content of the monster's stat block" },
        "lastUpdated": { "type": "string", "format": "date-time", "description": "When this monster's info was last cached" }
      },
      "required": ["slug", "name", "htmlContent"]
    }
  },
  "firestore": {
    "monsters/{monsterSlug}": {
      "schema": "Monster",
      "description": "Collection of cached monster information"
    }
  }
}
